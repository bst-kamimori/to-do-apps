```mermaid
erDiagram
    tasks ||--o{ project_names : ""
    project_names ||--o{ categories : ""
    categories ||--o{ operations : ""

    users {
        bigint id 
        varchar name 
	    varchar email
        varchar email_verified_at
        varchar password
        varchar remember_token
        timestamp created_at 
        timestamp updated_at
    }

    tasks {
        bigint id 
        varchar name 
        date start_date
        date end_date
        int progress
        text remarks
        timestamp created_at 
        timestamp updated_at
        tinyint is_completed
        int operation_id
        int project_name_id
        int category_id
        int is_recurring
        int recurrence
        int next_run_at
    }

    project_names {
        bigint id  
        timestamp created_at 
        timestamp updated_at
	    varchar name
    }

    categories {
        bigint id  
        timestamp created_at 
        timestamp updated_at
        varchar name
        int project_name_id
    }

    operations {
        bigint id  
        timestamp created_at 
        timestamp updated_at
        varchar name
        int category_id
    }
```
