```mermaid

%%ER図

erDiagram
    tasks ||--o{ project_names : ""
    project_names ||--o{ categories : ""
    categories ||--o{ operations : ""

    users {
        bigint id 
        varchar name 
	    varchar email
        varchar email_verified_at
        varchar password
        varchar remember_token
        timestamp created_at 
        timestamp updated_at
    }

    tasks {
        bigint id 
        varchar name 
        date start_date
        date end_date
        int progress
        text remarks
        timestamp created_at 
        timestamp updated_at
        tinyint is_completed
        int operation_id
        int project_name_id
        int category_id
        int is_recurring
        int recurrence
        int next_run_at
    }

    project_names {
        bigint id  
        timestamp created_at 
        timestamp updated_at
	    varchar name
    }

    categories {
        bigint id  
        timestamp created_at 
        timestamp updated_at
        varchar name
        int project_name_id
    }

    operations {
        bigint id  
        timestamp created_at 
        timestamp updated_at
        varchar name
        int category_id
    }
```

```mermaid
%%figmaで修正してPDF化した画面遷移図あり
%%フローチャートをLeft->Rightへ作成
flowchart LR
%%メイン画面
    Login["ログイン画面"]
    Dashboard["ダッシュボード"]
    TaskList["タスク一覧画面"]
    Completed["完了済み一覧画面"]

%%横のメイン導線
    Login --> Dashboard --> TaskList --> Completed

%%タスク関連（縦並び）
    subgraph TaskActions["タスク関連画面"]
        direction TB
        TaskCreate["タスク作成画面"]
        TaskEdit["タスク編集画面"]
        SearchResult["検索結果画面"]
    end

%%一覧からの枝分かれ
    TaskList --> TaskCreate
    TaskList --> TaskEdit
    TaskList --> SearchResult


```
